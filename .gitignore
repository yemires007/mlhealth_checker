# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/
.env

# user database (recreated at runtime)
*.db
*.sqlite3

# NOTE: model files (models/*.pkl) are intentionally NOT ignored —
# Render needs them committed to the repo to load at runtime.
